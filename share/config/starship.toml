"$schema" = 'https://starship.rs/config-schema.json'

command_timeout = 1500
palette = "rose-pine-moon"

format = """
$hostname\
$directory\
[](fg:overlay)$git_branch$git_status[](fg:overlay)\
$line_break$character"""

[battery]
disabled = true
[cmd_duration]
disabled = true

[palettes.rose-pine-moon]
overlay = '#393552'
love = '#eb6f92'
gold = '#f6c177'
rose = '#ea9a97'
pine = '#3e8fb0'
foam = '#9ccfd8'
iris = '#c4a7e7'
subtle = '#908caa'

[hostname]
ssh_only = false
style = "bg:overlay fg:subtle"
disabled = false
ssh_symbol = '🌐'
format = "[](fg:overlay)[   $hostname $ssh_symbol ]($style)[](fg:overlay) "

[character]
success_symbol = "[❯](gold)"
error_symbol = "[❯](gold)"
vimcmd_symbol = "[❮](overlay)"

[directory]
format = "[](fg:overlay)[ $path ]($style)[](fg:overlay) "
style = "bg:overlay fg:foam"
truncation_length = 2
truncation_symbol = "…/"

[git_branch]
format = "[ $symbol $branch ]($style)"
style = "bg:overlay fg:foam"
symbol = ""

[git_status]
disabled = false
style = "bg:overlay fg:subtle"
format = '([$all_status$ahead_behind]($style))'
