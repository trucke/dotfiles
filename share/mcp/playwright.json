{
  "_comment": "Canonical Playwright MCP server spec — headless Chromium for agents to drive/verify web apps they build (no display required). Standard `mcpServers` shape; command assumes the pnpm global bin dir is on PATH. `kratos/justfile playwright-mcp` registers this with Claude Code using an ABSOLUTE path for robustness. See README.md for per-agent wiring.",
  "mcpServers": {
    "playwright": {
      "command": "playwright-mcp",
      "args": ["--headless", "--browser", "chromium", "--isolated"]
    }
  }
}
